# 快速开始

本页用于验证文档站的基本布局、代码高亮与右侧目录（Outline）。

::: warning 内容待补充
本页目前是测试页面，完整的安装与配置说明会在后续补充。下面的命令仅为占位示例，用于验证代码高亮，请勿直接照搬执行。
:::

## 安装

安装方式待补充。以下代码块用于验证 shell 语法高亮：

```bash
# 占位示例：确认 mycode 命令是否可用
mycode --version
```

## 基础配置

MyCode Core 通过 machine runtime 驱动，具体配置项与启动参数待补充。以下代码块用于验证 PowerShell 语法高亮：

```powershell
# 占位示例：以 JSONL 模式启动并继续上一次会话
mycode runtime --jsonl --continue
```

## 下一步

- [MCP](/mcp/)
